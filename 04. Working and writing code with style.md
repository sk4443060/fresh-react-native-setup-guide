Working and writing code with style
Navigate into app/(tabs)/index.tsx
  Clean the default codes from this file and write your own code as:

    import { View, Text, StyleSheet } from 'react-native'
    const app = () => {
      return (
      <View style={styles.container}>
        <Text style={styles.text}>Coffee Shop</Text>
      </View>
      )
    }
    export default app

    const styles = StyleSheet.create({
      container: {
        flex: 1,
        flexDirection: 'column',
      },
      text: {
        color: white,
        fontSize: 42,
        fontWeight: 'bold',
        textAlign: 'center',
        
      }
    })
